#include <stdio.h>

int main() {
    char a[100]; // Declare a character array with a maximum size of 100 characters
    scanf("%s", a); // Use %s for string input
    printf("You entered: %s\n", a); // Use %s for string output
    return 0;
}
